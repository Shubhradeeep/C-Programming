/* ADDITION sub div multi OF int
modulo division = REMANING AFTER DIVISION SIGN % */

#include<stdio.h>
int main()
{  
 int a,b,c,d,e,f,g;
printf("enter two numbers ");
scanf("%d%d",&a,&b); 

c=a+b;
printf("\n addition of two numbers,%d",c);

d=a-b;
printf("\n subtraction of two numbers,%d",d);

e=a/b;
printf("\n int division of two numbers,%d",e);

f=a*b;
printf("\n multiplication of two numbers,%d",f);

g=a%b;
printf("\n modulo division of two numbers,%d",g);





return 0;
}
